{
  "name": "ta-management-solution",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postinstall": "prisma generate",
    "prisma:migrate": "prisma db push --accept-data-loss",
    "start": "npm run prisma:migrate && next start -p $PORT",
    "lint": "next lint"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "next": "14.2.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "eslint": "8.56.0",
    "eslint-config-next": "14.2.3",
    "prisma": "^6.18.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
